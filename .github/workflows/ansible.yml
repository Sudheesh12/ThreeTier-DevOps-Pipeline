name: "Ansible Workflow"

on:
  workflow_run:
    workflows: ["Terraform"]
    types:
      - completed

jobs:
  ansible:
    name : 'ansible'
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
    - name: Checkout
      uses: actions/checkout@v4
    


    
  

    

