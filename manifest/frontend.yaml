apiVersion: apps/v1

kind: Deployment

metadata:
  name: frontend
  namespace: 3-tier-app
  labels:
    name: frontend
    project: 3-tier-app
    env: test

spec:
  replicas: 1
  selector:
    matchLabels:
      name: frontend

  template:
    metadata:
      labels:
        name: frontend
    spec:
      containers:
      - name: nginx-frontend
        image: sudheesh12/frontend
        ports:
        - containerPort: 80


---

apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: NodePort
  selector:
    name: frontend
  ports:
  - port: 80
    nodePort: 30000